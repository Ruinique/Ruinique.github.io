import{_ as a,o as e,c as o,a as d}from"./app-hsNDf8jE.js";const r={},c=d('<h2 id="java-内存区域划分" tabindex="-1"><a class="header-anchor" href="#java-内存区域划分" aria-hidden="true">#</a> Java 内存区域划分</h2><p>关于 Java 内存区域的划分，我觉得 《深入理解 Java 虚拟机》 里面的有一张图很灵魂： <img src="https://image.itbaima.cn/images/336/image-20240306145338590.png" alt="image.png"> 我们可以分类去记忆，首先是有 <strong>程序计数器</strong>，他相当于我们计算机组成原理里面学到的 PC 指针一样，指向我们要执行的字节码代码，利用程序计数器，我们就可以完成线程切换，为了保证我们可以完成线程切换，能记住切换前我在跑哪一条字节码代码，每个线程都有自己的程序计数器。除了程序计数器以外，还有一个线程私有的就是 <strong>Java 虚拟机栈</strong>了，虚拟机栈就很接近传统 cpp 栈的概念了，每当一个 字节码函数 执行的生命周期，本质上就是函数栈帧从入栈到出栈的过程，函数的栈帧里面包括了动态链接，操作数栈，局部变量表和方法出口这些内容。当然，我们关键关注的还是 局部变量表，里面包含了编译期可以确定的量：对象引用和 <code>returnAddress</code>。对于 Java 虚拟机，为了提高其运行效率，我们还提供了很多 <code>native</code> 方法，所以与 Java 虚拟机栈相对，我们还有我们的 <strong>本地方法栈</strong>，他也是线程私有的。</p><p>然后就是共用的 <strong>堆</strong> 和 <strong>方法区</strong> 了，我们几乎所有的对象实例都分配在堆上，我们的线程私有的 栈 存储的只是其上的引用，详细可以看垃圾回收的部分。<strong>方法区</strong>也是我们多个线程共享的区域，这部分主要存储我们的运行时常量池，其是一组编译时期产生的符号引用和字面量常量的有序集合，但是也可以包含新的常量，比如 <code>String</code> 类的<code>intern()</code>方法 。它包含类、接口、方法和字段的引用，以及字符串、数值常量等，而类的这些信息放入我们的方法区则是通过类加载的过程实现的。 除了上面的内存区域以外，我们就涉及到<strong>直接内存</strong>了，在 Java NIO 中，我们提供了操作直接内存的方法，这就为我们实现<strong>零拷贝</strong>提供了帮助，减少了我们在Java堆和Native堆中来回复制数据，比如读取某文件数据写到网络套接字，我们不需要先把内容读取成字节数组（数组是在堆上的），然后再写入我们的网络套接字，可以直接利用操作直接内存的方法直接将文件内容写入到我们的网络套接字中。</p><h2 id="内存溢出异常" tabindex="-1"><a class="header-anchor" href="#内存溢出异常" aria-hidden="true">#</a> 内存溢出异常</h2><p>除了我们的 程序计数器 以外，其他部分都存在对应的内存溢出异常。对于我们线程私有的栈，如果是可以动态拓展的，申请不到内存的时候也会报错 <code>OutOfMemoryError</code>；但是我们一般针对的 <code>HotSpot</code> ，其内存不能动态拓展，所以我们的栈只会因为深度不足爆栈 <code>StackOverflowError</code>；而对于我们的堆和方法区，我们可以去指定其内存大小，在不够用的时候也会报错 <code>OutOfMemoryError</code>。最后，直接内存当然也有大小限制，也就实际上也存在 <code>OutOfMemoryError</code>。</p><h2 id="java-对象创建过程" tabindex="-1"><a class="header-anchor" href="#java-对象创建过程" aria-hidden="true">#</a> Java 对象创建过程</h2><p>Java 在创建的过程中，会先去看我们的运行时常量池里面有没有这个类的加载信息，这和我们的类加载过程息息相关。</p><p>当进行完类加载之后，我们就开始分配内存了，分配内存的操作有两种，一种是通过指针碰撞的方式，也就是直接分配一段连续的内存，而如果内存的碎片很多，我们就只能通过 <code>Free List</code> 去分配我们的内存了。这个到底是采取哪一种方式，和我们的垃圾回收机制有关。 当然，既然堆上的内存是多个线程共享的，那么就会有并发的问题，我们既可以直接通过 CAS 保证，也可以走 TLAB 策略（分配给每个线程 buffer，只有分配 buffer 的时候才需要保证同步）。</p><p>最后，我们会清除对象内存的信息和配置对象头的信息，对象头里有哪些内容我们接下来介绍对象结构的时候会介绍。</p><h2 id="java-对象内存布局" tabindex="-1"><a class="header-anchor" href="#java-对象内存布局" aria-hidden="true">#</a> Java 对象内存布局</h2><p>Java 的对象主要分成这样几个部分：对象头，实例数据和 Paddings。 关于对象头其实深入理解 Java 虚拟机说的也有些含糊不清，先看这个图： <img src="https://image.itbaima.cn/images/336/image-20240306162460946.png" alt="image.png"> 我们的 <code>hashCode</code> 实际上是懒加载的，所以在无锁状态下，我们会将其放到对象头里，除此以外，我们有如下规则</p><ul><li>当一个对象已经计算过<code>identity hash code</code>，它就无法进入偏向锁状态；</li><li>当一个对象当前正处于偏向锁状态，并且需要计算其 <code>hash code</code>的话，则它的偏向锁会被撤销，并且锁会膨胀为重量锁；</li><li>重量锁的实现中，<code>ObjectMonitor</code>类里有字段可以记录非加锁状态下的<code>mark word</code>，其中可以存储 <code>hash code</code> 的值。或者简单说就是重量锁可以存下 <code>hash code</code>。</li></ul><p>接下来就是实例信息，实例数据部分是对象真正存储的有效信息，无论是从父类继承下来的，还是在子类中定义的字段都必须记录起来。我们按相同宽度的字段放在一起存放，然后父类的字段总在子类的字段之前，当然，在开启某些配置的情况下，会让子类”见缝插针“。最后，<code>hotspot</code> 要保障对象是 8字节 的整数倍，所以，还会有 <code>padding</code> 填充我们的对象，保证他能被 8 字节整除。</p><h2 id="java-对象访问" tabindex="-1"><a class="header-anchor" href="#java-对象访问" aria-hidden="true">#</a> Java 对象访问</h2><p>Java 的对象访问主要有两种，一种是在堆上分配一个句柄池，句柄里面同时包含了对象的实例数据的位置和类相关信息在方法区中的位置，而更常见的方法则是放在对象的实例数据当中有类相关的信息在方法区中的位置，<code>Hotspot</code> 的大部分的实现都是这样完成的。</p>',15),t=[c];function n(i,s){return e(),o("div",null,t)}const p=a(r,[["render",n],["__file","JVM-1.html.vue"]]);export{p as default};
