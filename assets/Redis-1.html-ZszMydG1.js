import{_ as e,o as d,c,a as o}from"./app-XSDy5BHM.js";const i={},a=o('<h2 id="redis-的线程模型和-epoll-网络框架" tabindex="-1"><a class="header-anchor" href="#redis-的线程模型和-epoll-网络框架" aria-hidden="true">#</a> Redis 的线程模型和 <code>epoll</code> 网络框架</h2><p>既然要整理我们 <code>Redis</code> 为什么高性能，那么我们 <code>Redis</code> 在处理层的 I/O 模型和线程模型就不得不介绍了。</p><p>常说 <code>Redis</code> 是单线程的，是指 <code>Redis</code> 的网络IO和键值对读写是由一个线程来完成的（其实还有很多操作用了其他线程），这也是 <code>Redis</code> 对外提供键值存储服务的主要流程。为什么我们 <code>Redis</code> 的网络 IO 和键值对读写使用一个线程完成了，主要为了防止“加锁”导致的访问并发控制使得我们的吞吐量上不去。而 <code>Redis</code> 为什么单线程还能保证高性能的吞吐呢？主要是运用了 I/O 多路复用的思想。这一点我们在学习 <code>Netty</code> 的时候肯定也学过，他的事件驱动模型实际上一个 单线程的 <code>Reactor</code> 模型 。</p><p>要介绍 <code>Reactor</code> 模型，我们就要从多路复用说起并介绍 <code>select</code>、<code>poll</code> 和 <code>epoll</code> 这三种不同的机制了。我们先从 <code>socket</code> 模型讲起，<code>socket</code> 模型中调用 <code>socket</code> 我们可以得到一个主动套接字，在 <code>bind()</code> 绑定端口和地址后就调用 <code>listen()</code> 去做监听，这时候会返回监听套接字，监听套接字又可以被 <code>accept()</code> 处理变成已连接套接字，然后就可以进行 I/O 读写了。所谓的 I/O 多路复用的意思就是，我们允许在单线程存在多个监听套接字和已连接套接字。</p><p>但是实际上 <code>accept()</code> 或者 <code>connect()</code> 这种都是阻塞的，这样某个实际上没有影响我们业务的请求有可能也会影响我们整体的事件处理，我们可以用 <code>select()</code> 或者 <code>epoll()</code> 这种非阻塞的方式去处理我们的监听套接字。<code>select()</code> 函数使用三个我们传入的数组存我们读、写和连接三个 <code>FD_SET</code> 里面，而 <code>poll()</code> 其实和 <code>select()</code> 函数在底层实现上调用的是一个函数，只是他的 <code>FD_SET</code> 可以自己指定，也就不受 1024 宏定义的影响。最后，我们介绍 <code>epoll()</code> ，<code>epoll()</code> 用回调代替了轮询，所以可以 O(1) 找到需要的监听套接字，吞吐量大得多，<code>epoll()</code> 只要将套接字放到红黑树上，就可以被内核访问，减少了一次复制，并且有了就绪的状态，对应的套接字就绪后会被放到链表中，并且添加了边缘触发模式，可以在只有状态发生变化的时候才会通知应用程序。</p><p>总而言之，<code>redis</code> 在 Linux 上使用 <code>epoll()</code> 实现了 I/O 的多路复用，这样，我们就可以处理多个客户端的连接和 I/O 请求了。在 Redis 6.0 前，Redis 所有的事件处理都是单线程的，但是后来为了防止 CPU 被打满，我们会在处理事件的时候把 <code>I/O 事件</code> 交给多线程去做，为此，特别引入了 I/O 事件的概念，用读事件、写事件和屏障事件保障了执行的有序，但是核心的事件处理还是单线程的 <code>Reactor</code> 模型。</p><h2 id="redis-的特殊数据结构" tabindex="-1"><a class="header-anchor" href="#redis-的特殊数据结构" aria-hidden="true">#</a> Redis 的特殊数据结构</h2><h3 id="redisobject" tabindex="-1"><a class="header-anchor" href="#redisobject" aria-hidden="true">#</a> <code>redisObject</code></h3><p><strong><code>redisObject</code></strong> 是 Redis 类型系统的<strong>核心</strong>, 数据库中的每个键、值，以及Redis 本身处理的参数，都表示为这种数据类型。首先 Redis 结构头部的很多内容使用位域定义方法实现的。除此之外，对于小于等于 44 字节的字符串在创建完 <code>SDS</code> 以后会直接把 redisObject 中的指针 <code>ptr</code>，指向 <code>SDS</code> 结构中的字符数组，这样就能让这两内存实际上连续，从而避免内存碎片和两次内存分配的开销了。</p><h3 id="sds" tabindex="-1"><a class="header-anchor" href="#sds" aria-hidden="true">#</a> <code>SDS</code></h3><p>Redis 设计了 <code>SDS</code> 的结构，用来表示字符串。相比于 C 语言中的字符串实现，SDS 这种字符串的实现方式，会提升字符串的操作效率，并且可以用来保存二进制数据。char * 不能保存任意的值，并且必须遍历得到 <code>\\0</code> 才可以获取长度，因此操作总效率很低。SDS 其采用预分配冗余空间的方式来减少内存的频繁分配。</p><p><img src="https://image.itbaima.cn/images/336/image-20240307177667.png" alt="image.png"></p><p>SDS 的结构如上图所示：其中SDS 的结构里包含了一个字符数组 buf[]，用来保存实际数据。同时，SDS 结构里还包含了三个元数据，分别是字符数组现有长度 len、分配给字符数组的空间长度 alloc，以及 SDS类型 flags。这个包含元数据的头的大小也是可变的，由 sdshdr 决定。在编译上也采用了对应的优化。</p><h3 id="hashmap" tabindex="-1"><a class="header-anchor" href="#hashmap" aria-hidden="true">#</a> <code>HashMap</code></h3><p>为了实现从键到值的快速访问，<code>Redis</code> 使用了一个哈希表来保存所有键值对。一个哈希表，其实就是一个数组，数组的每个元素称为一个哈希桶。类似我们的 <code>HashMap</code>。我们的值并不是直接是值，而是一个指向具体值的指针。我们可以用 key 计算应该到哪个哈希桶再找到对应的元素。</p><p>当然，当数据增多的时候会受到哈希冲突和 rehash 开销的影响，针对哈希冲突，<code>Redis</code> 采用了链式哈希。对于 rehash 开销，Redis 实现了渐进式 rehash 设计。</p><p>链式哈希就是用一个链表把映射到 Hash 表同一桶中的键给连接起来。为了实现链式哈希， Redis 在每个 <code>dictEntry</code> 的结构设计中，除了包含指向键和值的指针，还包含了指向下一个哈希项的指针。当然，我们也要注意到，键值对的值是由一个联合体 v 定义的，可以节省内存。那就是随着链表长度的增加，Hash 表在一个位置上查询哈希项的耗时就会增加，从而增加了 Hash 表的整体查询时间，这样也会导致 Hash 表的性能下降。</p><p>因此，如果当元素个数大于桶数时，就会准备开始 rehash 并进行扩容，当元素个数大于桶数 5 倍时，就会强制进行扩容，这种扩容我们称之为 rehash，接下来介绍 Redis 是怎么实现 rehash 的。</p><p>Redis 在 dict.h 文件中使用 <code>dictht </code>结构体定义了 Hash 表。不过，在实际使用 Hash 表时，Redis 又在 <code>dict.h</code> 文件中，定义了一个 dict 结构体。这个结构体中有一个数组（ht[2]），包含了两个 Hash 表 ht[0]和 ht[1]。这个我们称之为全局 <code>hash</code> 表。正常的操作针对 ht[0] ，而迁移到 ht[1] ，迁移完成后释放 ht[0] ，并把 ht[1] 赋给 ht[0]。</p><p>rehash 在当前没有 RDB 子进程，并且也没有 AOF 子进程的时候，会当元素个数大于桶数时，就会准备开始 rehash 并进行扩容。当元素个数大于桶数 5 倍时，就会强制进行扩容，这种扩容我们称之为 <code>rehash</code>，扩容就是变为 2 倍。</p><p>由于 Hash 表空间扩大，原本映射到某一位置的键可能会被映射到一个新的位置上，因此，很多键就需要从原来的位置拷贝到新的位置。而在键拷贝时，Redis 主线程无法执行其他请求，所以键拷贝会阻塞主线程，这样就会产生 <code>rehash</code> 开销。渐进式 <code>rehash</code> 的意思就是 Redis 并不会一次性把当前 Hash 表中的所有键，都拷贝到新位置，而是会分批拷贝，每次的键拷贝只拷贝 Hash 表中一个 <code>bucket</code> 中的哈希项。对于非空桶，每迁移完一个 <code>bucket</code>，就会处理其他事情。</p><h3 id="ziplist" tabindex="-1"><a class="header-anchor" href="#ziplist" aria-hidden="true">#</a> ziplist</h3><p>创建 ziplist 的逻辑很简单，就是创建一块连续的内存空间，大小为 <code>ZIPLIST_HEADER_SIZE</code> 和 <code>ZIPLIST_END_SIZE</code> 的总和，然后再把该连续空间的最后一个字节赋值为 ZIP_END，表示列表结束。其头部包含四字节的 ziplist 字节数，尾部偏移量，元素数目。ziplist 列表项包括三部分内容，分别是前一项的长度（prevlen）、当前项长度信息的编码结果（encoding），以及当前项的实际数据（data）。而对于每一个列表项，维护的对应信息又可以进行对应的压缩，这就是所谓 ziplist。</p><h3 id="intset" tabindex="-1"><a class="header-anchor" href="#intset" aria-hidden="true">#</a> intset</h3><p>他是一个结构体，包含了 4字节的编码结果和长度，只不过对于每一项直接使用 int8_t 类型的整数数组。</p><h3 id="quicklist" tabindex="-1"><a class="header-anchor" href="#quicklist" aria-hidden="true">#</a> quicklist</h3><p>虽然 ziplist 通过紧凑的内存布局来保存数据，节省了内存空间，但是 ziplist 也面临着随之而来的两个不足：查找复杂度高和潜在的连锁更新风险。当要查找列表中间的元素时，ziplist 就得从列表头或列表尾遍历才行。而且我们在非末尾处插入值时，会使得下一项的 prevlen 发生改变，prevlen 发生改变后又会改变下一个 prevlen，发生了大规模的更新。</p><p>因此 redis 引入了 quicklist，一个 quicklist 就是一个链表，而链表中的每个元素又是一个 ziplist。我们通过 quicklist 这种结构，保证每个 ziplist 不会太大，并且可以快速找到中间的元素。</p><h3 id="listpack" tabindex="-1"><a class="header-anchor" href="#listpack" aria-hidden="true">#</a> listpack</h3><p>listpack 也叫紧凑列表，它的特点就是用一块连续的内存空间来紧凑地保存数据，同时为了节省内存空间，listpack 列表项使用了多种编码方式，来表示不同长度的数据，这些数据包括整数和字符串。</p><p>listpack 虽然并没有 prevlen ，但是它使用 IpPrev 也可以获取到前一项的长度。IpPrev 使用读取 entry-len 的方式得到前一项的长度。我们可以先计算本字节的长度，再向左偏移找到左边一项的 entry-len。</p><h3 id="zset" tabindex="-1"><a class="header-anchor" href="#zset" aria-hidden="true">#</a> zset</h3><p>有序集合（Sorted Set）是 Redis 中一种重要的数据类型，它本身是集合类型，同时也可以支持集合中的元素带有权重，并按权重排序。Sorted Set 能支持范围查询，这是因为它的核心数据结构设计采用了跳表，而它又能以常数复杂度获取元素权重，这是因为它同时采用了哈希表进行索引。</p><p>跳表其实是一种多层的有序链表。在课程中，为了便于说明，我把跳表中的层次从低到高排个序，最底下一层称为 <code>level0</code>，依次往上是 <code>level1</code>、<code>level2</code> 等。</p><p>由于跳表结点同时保存了元素和权重，所以跳表在比较结点时，相应地有两个判断条件：</p><ol><li>当查找到的结点保存的元素权重，比要查找的权重小时，跳表就会继续访问该层上的下一个结点。</li><li>当查找到的结点保存的元素权重，等于要查找的权重时，跳表会再检查该结点保存的 <code>SDS</code> 类型数据，是否比要查找的 SDS 数据小。如果结点数据小于要查找的数据时，跳表仍然会继续访问该层上的下一个结点。</li><li>但是，当上述两个条件都不满足时，跳表就会用到当前查找到的结点的 <code>level</code> 数组了。跳表会使用当前结点 <code>level </code>数组里的下一层指针，然后沿着下一层指针继续查找，这就相当于跳到了下一层接着查找。</li></ol><p>我们一般维持每一层结点数都约是下一层结点数的一半，不过 <code>Redis</code> 定的概率是 0.5。</p><p><code>Sorted Set</code> 先是通过在它的数据结构中同时定义了跳表和哈希表，来实现同时使用这两种索引结构。然后，<code>Sorted Set</code> 在执行数据插入或是数据更新的过程中，会依次在跳表和哈希表中插入或更新相应的数据，从而保证了跳表和哈希表中记录的信息一致，共同维护 <code>zset</code>。</p><h3 id="radixtree" tabindex="-1"><a class="header-anchor" href="#radixtree" aria-hidden="true">#</a> RadixTree</h3><p><code>Redis</code> 从 5.0 版本开始支持提供 <code>Stream</code> 数据类型，可以用来保存消息数据，实现一个带有消息读写基本功能的消息队列。</p><p><code>Stream</code> 作为消息队列，一般拥有两个特征：</p><ul><li>每个消息由键值对组成</li><li>每个消息对应一个消息 <code>id</code></li></ul><p>而对于消息 <code>ID</code> 我们一般能让 <code>Redis</code> 自动生成递增的消息 <code>ID</code>，由时间戳和序号，这时他往往有大量的相同前缀，因此，我们可以使用前缀树来保存 <code>id</code>，而具体数据用 <code>ziplist</code> 来保存。前缀树的根节点不保存任何字符，而除了根节点以外的其他节点，每个节点只保存一个字符。相对于一般的前缀树，<code>RadixTree</code> 如果一系列单字符节点之间的分支连接是唯一的，那么这些单字符节点就可以合并成一个节点，这样既可以节约内存的使用，同时还可以提高查询访问的效率。</p><p><code>Radix Tree</code> 会指明本节点以上内容是否为 key，本节点是否为 <code>NULL</code>，本节点是否为压缩节点，本节点的具体大小。在实际存储中，如果表示完整的一个 key，那么它也将包含节点为 key 时的 value 指针。</p><p>为了满足内存对齐的需要，<code>raxNode</code> 会根据保存的字符串长度，在字符串后面填充一些字节。<code>Radix Tree</code> 本身也是有序的树型索引，可以支持单点和范围查询。</p><p>所以，<code>Redis</code> 把消息 <code>ID</code> 保存在 <code>Radix Tree</code> 中，既可以节省内存空间，也能高效支持消息 <code>ID</code> 的查询。而 <code>listpack</code> 本身是紧凑列表，在保存大量消息内容的同时，也能有效节省内存。</p>',46),s=[a];function t(r,h){return d(),c("div",null,s)}const l=e(i,[["render",t],["__file","Redis-1.html.vue"]]);export{l as default};
