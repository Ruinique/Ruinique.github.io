import{_ as e,o as d,c,a as o}from"./app-hsNDf8jE.js";const a={},i=o('<h2 id="aof-机制" tabindex="-1"><a class="header-anchor" href="#aof-机制" aria-hidden="true">#</a> AOF 机制</h2><p><code>Redis</code> 建立在内存上，一旦宕机数据就会全部失效，那我们就需要持久化。我们知道 <code>Mysql</code> 的日志存在 <code>WAL</code> 机制，也就是我们先写日志，再做操作。但是我们的 <code>AOF</code> 是执行后再写日志，并且和 <code>binlog</code> 一样是逻辑日志（记录的是命令），这样就可以避免检查我们的命令，我们的 <code>AOF</code> 又是在主线程执行的，执行后写日志还可以避免阻塞现在的写操作。不过，即使是执行后再写日志，依然会导致 <code>AOF</code> 操作阻塞主线程，特别是在落盘速度很慢的时候，所以，<code>Redis</code> 默认不开启 <code>AOF</code> ，除此之外，我们还可以按需选择每秒同步一次和每个操作都同步。除了这个问题以外，如果刚执行完一个命令，还没有来得及记日志就宕机了，那么这个命令和相应的数据就有丢失的风险，所以我们不会用 <code>AOF</code> 来做主从同步。</p><p>当 <code>AOF</code> 的文件过大以后，我们在后台可以对 <code>AOF</code> 执行重写操作，虽然还是逻辑日志，但是只保证最终的结果一致就可以了。不过，<code>AOF</code> 重写的过程中，我们既然是后台进行的，那么主线程就仍在接收请求，这时候我们的 <code>AOF</code> 依然在工作，这时候我们有需要被重写的日志和正在重写生成的日志，我们的记录会同时写入这两个日志的缓冲区。这样如果重写成功，我们重写后的 <code>AOF</code> 文件里也记录了这段操作；如果重写失败，且发生宕机，这段时间的操作也不会因为重写丢失。</p><h2 id="rdb-机制" tabindex="-1"><a class="header-anchor" href="#rdb-机制" aria-hidden="true">#</a> RDB 机制</h2><p>RDB 是 <code>Redis</code> 持久化的另一种方式，Redis的数据都在内存中，为了提供所有数据的可靠性保证，它执行的是<strong>全量快照</strong>。Redis提供了两个命令来生成RDB文件，分别是<code>save</code>和<code>bgsave</code>。<code>bgsave</code> 会创建一个子进程，专门用于写入RDB文件，避免了主线程的阻塞。</p><p>不过既然我们要做的是某一时刻的快照，快照的保存又是在后台执行的。那么不可避免的就会出现写操作的问题，在后台执行 RDB 快照的时候，如果主线程接收到写操作，会利用操作系统提供的写时复制的能力，如果某块数据需要进行写操作，那么我们就会将这块数据做一个备份再执行写操作，我们快照只需保存备份的数据就行了。</p><p>那么我们能不能为了数据不丢失不断地进行 RDB 快照呢？显然是不能的，虽然 RDB 是在后台执行的，但是频繁对磁盘的写入会导致磁盘压力很大，并且 <code>fork</code> 线程进行 RDB 备份的过程也是会阻塞主线程的。不过，如果我们用增量快照就能避免频繁对磁盘的写入，可这样为了记录哪些是发生变动的开销也会很大。</p><p>所以 <code>Redis 4.0</code> 后给出的一个较为优秀的方案是用 <code>AOF</code> 去做两次全量快照之间的增量快照，利用两种方案的优点。</p><h2 id="主从复制" tabindex="-1"><a class="header-anchor" href="#主从复制" aria-hidden="true">#</a> 主从复制</h2><p>所谓的高可靠，我们要求的是出问题的时候数据丢失的比较少，服务中断的时间比较短，我们可以通过上面介绍的持久化方案来解决数据丢失的问题。但是为了在数据恢复的时候仍能保持服务，<code>Redis</code> 还设计了一主多从的模式，然后通过读写分离的方式（只在主库写，所有库都可以读）来避免复杂的版本控制。既然采用读写分离的方式，那么从库要保持和主库一致，就要从主库同步更新的数据，接下来我们介绍一下这个同步的流程，也就是“主从复制”。</p><p>当 Redis 实例向其他 Redis 实例发送 <code>slaveof</code> 或者 <code>replicaof</code> 命令后，就可以实现作为其他 Redis 实例的从库了。然后，从库会向主库发送 <code>psync ? -1</code> 的命令，<code>?</code> 是 <code>runID</code>，-1 则是表明是第一次复制。此时主库会向从库发送 <code>FULLRESYNC</code> 、<code>runID</code> 和 <code>offset</code>，从库会记下来，从而以后让主库知道没找错人，第一次同步到哪了。然后主库会把对应进度的 RDB 发过去，此时从库加载这个 RDB。</p><p>之后的同步，都是从库根据在主库中自己的 <code>replication buffer</code> 传过来并执行去做的，这个 <code>replication buffer</code> 是啥呢？无论是和从库或者和客户端通信，客户端是一个<code>client</code>，从库也是一个<code>client</code>，我们每个<code>client</code>连上<code>Redis</code>后，<code>Redis</code>都会分配一个<code>client buffer</code>，所有数据交互都是通过这个<code>buffer</code>进行的：<code>Redis</code>先把数据写到这个<code>buffer</code>中，然后再把<code>buffer</code>中的数据发到<code>client socket</code>中再通过网络发送出去。如果从库读的速度是在太慢，我们的这个 <code>buffer</code> 也不能无限增长，到达某个特定参数后就会自动断开连接。</p><p>如果发生了上面的情况我们要怎么办呢？出现网络中断后，我们的 <code>replication buffer</code> 就没有在使用了，但是我们的 <code>repl_backlog_buffer</code> 还在使用。主库和从库都知道自己在这个环形缓冲区相对于起始位置的偏差，那么我们只要同步这两个指针中间的 <code>repl_backlog_buffer</code> 的内容就行了。当然，如果断开时间太长，主库有可能会因为这个缓冲区是环形的覆盖自身之前的记录，从而不得不全量复制。</p><p>为了避免多个从库同时和主库进行全量复制，给主库过大的同步压力，我们也可以采用“主-从-从”这一级联模式，来缓解主库的压力，也就是把一些从库设置成其他从库的主库。</p><h2 id="哨兵机制" tabindex="-1"><a class="header-anchor" href="#哨兵机制" aria-hidden="true">#</a> 哨兵机制</h2><p>我们发现即使有了主从的机制，可能还是存在问题：如果我们的主库了怎么办呢？这时候我们就要引入 哨兵机制 了。哨兵其实就是一个运行在特殊模式下的Redis进程，主从库实例运行的同时，它也在运行。哨兵主要负责的就是三个任务：监控、选主（选择主库）和通知。哨兵通过 PING 命令知道主库和存库是否下线，如果从库下线，标记一下就可以了；如果是主库下线了，我们就要执行选主，选主完成后通知其他从库向新主库发起 <code>replicaof</code>。</p><p>监控哨兵出现误判怎么办？这就要求我们用哨兵集群去完成这个工作。哨兵集群又是怎么运行的呢？<code>Redis</code> 有发布-订阅的机制：在主从集群中，主库上有一个名为“<code>__sentinel__:hello</code>”的频道，不同哨兵就是通过它来相互发现，实现互相通信的。接下来我们的哨兵可以通过向主库请求得到从库地址，从而完成监控。像标记主观下线、客观下线、通知进行主从切换，其实也是 <code>Redis</code> 实例客户端通过订阅-发布机制实现的。</p><p>选主的规则是什么呢？首先筛掉总是和主库断连，而且断连次数超出了一定的阈值的从库，然后我们依次按指定优先级，和主库同步程度以及 ID 字段序完成选主的排序。</p>',18),r=[i];function s(n,t){return d(),c("div",null,r)}const f=e(a,[["render",s],["__file","Redis-2.html.vue"]]);export{f as default};
